# Ask AI. Without this the site builds and search still works; the Ask AI panel
# answers with a message saying it is not configured.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4.1-mini.
OPENAI_DOCS_MODEL=
