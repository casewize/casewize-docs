/**
 * Nextra resolves MDX components through this file. The docs theme's own set is
 * the whole of it — anything custom would go in the spread here.
 */
export { useMDXComponents } from "nextra-theme-docs"
